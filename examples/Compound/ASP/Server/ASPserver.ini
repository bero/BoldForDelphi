; Database settings for the ASP example's ISAPI server.
;
; Must point at the database DBGenerator created. A bare file name is taken as
; relative to ASPserver.dll, not to the IIS worker process.

[SQLite]
Database=ASPDemo.db
